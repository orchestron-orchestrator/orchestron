# WARNING WARNING WARNING WARNING WARNING
# DO NOT MODIFY THIS FILE!! This file is generated!
# WARNING WARNING WARNING WARNING WARNING

import logging
import xml
import orchestron.ttt as ttt
import yang.adata
import yang.gdata
import yang.xml

from mini.layers.y_1 import orchestron_rfs__rfs__base_config
from mini.layers.y_1 import orchestron_rfs__rfs__bar_entry
from mini.layers.y_1 import orchestron_rfs__rfs__bar__dynstate
from mini.layers.y_1 import SRC_DNODE
from mini.layers.y_2_loose import root as output_root

def o_root():
    return output_root()


class BaseConfig(ttt.RFSFunction):
    transform: mut(orchestron_rfs__rfs__base_config, ttt.DeviceInfo) -> yang.adata.MNode

    @staticmethod
    def input_type():
        """Return the modeled input type for this transform"""
        return orchestron_rfs__rfs__base_config

    mut def transform_wrapper(self, i: yang.gdata.Node, device_info: ttt.DeviceInfo, memory: ?yang.gdata.Node, dynstate: ?yang.gdata.Node) -> (yang.gdata.Node, ?yang.gdata.Node):
        """Wrap the user provided transform method to convert from gdata to
        modeled input and back to gdata
        """
        modeled_input = orchestron_rfs__rfs__base_config.from_gdata(i)

        return self.transform(modeled_input, device_info).to_gdata(), None

    mut def transform_xml(self, i: xml.Node, device_info: ttt.DeviceInfo, memory: ?yang.gdata.Node, dynstate: ?yang.gdata.Node) -> (yang.gdata.Node, ?yang.gdata.Node):
        """Wrap the user provided transform method to convert from XML to
        modeled input and return gdata
        """
        gdata_input = yang.xml.from_xml(SRC_DNODE, i, loose=True, root_path=['orchestron-rfs:rfs', 'mini-rfs:base-config'])
        modeled_input = orchestron_rfs__rfs__base_config.from_gdata(gdata_input)

        return self.transform(modeled_input, device_info).to_gdata(), None

class Bar(ttt.RFSFunction):
    transform: mut(orchestron_rfs__rfs__bar_entry, ttt.DeviceInfo, dynstate: ?orchestron_rfs__rfs__bar__dynstate) -> yang.adata.MNode

    @staticmethod
    def input_type():
        """Return the modeled input type for this transform"""
        return orchestron_rfs__rfs__bar_entry

    mut def transform_wrapper(self, i: yang.gdata.Node, device_info: ttt.DeviceInfo, memory: ?yang.gdata.Node, dynstate: ?yang.gdata.Node) -> (yang.gdata.Node, ?yang.gdata.Node):
        """Wrap the user provided transform method to convert from gdata to
        modeled input and back to gdata
        """
        modeled_input = orchestron_rfs__rfs__bar_entry.from_gdata(i)
        modeled_dynstate = orchestron_rfs__rfs__bar__dynstate.from_gdata(dynstate) if dynstate is not None else None

        return self.transform(modeled_input, device_info, modeled_dynstate).to_gdata(), None

    mut def transform_xml(self, i: xml.Node, device_info: ttt.DeviceInfo, memory: ?yang.gdata.Node, dynstate: ?yang.gdata.Node) -> (yang.gdata.Node, ?yang.gdata.Node):
        """Wrap the user provided transform method to convert from XML to
        modeled input and return gdata
        """
        gdata_input = yang.xml.from_xml(SRC_DNODE, i, loose=True, root_path=['orchestron-rfs:rfs', 'mini-rfs:bar'])
        modeled_input = orchestron_rfs__rfs__bar_entry.from_gdata(gdata_input)
        modeled_dynstate = orchestron_rfs__rfs__bar__dynstate.from_gdata(dynstate) if dynstate is not None else None

        return self.transform(modeled_input, device_info, modeled_dynstate).to_gdata(), None
